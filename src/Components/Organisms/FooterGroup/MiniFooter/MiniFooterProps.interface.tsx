export interface MiniFooterProps {
  copyrightText: string; // Copyright text to display in the footer
  links: { label: string; url: string }[]; // An array of links to display in the footer
}

export default MiniFooterProps;
