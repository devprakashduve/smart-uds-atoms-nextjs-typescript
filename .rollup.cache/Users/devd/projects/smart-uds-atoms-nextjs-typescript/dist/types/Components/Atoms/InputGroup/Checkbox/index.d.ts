import React from 'react';
import { CheckboxProps } from './CheckboxProps.interface';
declare const Checkbox: ({ checked, label, toggleChecked, size, title, name, disabled, indeterminate, }: CheckboxProps) => React.JSX.Element;
export default Checkbox;
